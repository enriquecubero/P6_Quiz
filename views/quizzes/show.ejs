
<h1>
    Mostrar pregunta:
</h1>

<div class="quizItem">

    <div>
        <% var attachment = quiz.attachment; %>
        <% include ../attachments/_show %>
    </div>

    <div>
        <% include ../favourites/_stars.ejs %>
        <br/>
        Question: <b><%= quiz.question %></b>
        <br />
        Answer: <b><%= quiz.answer %></b>
        <br />
        <small> by <%= quiz.author ? quiz.author.username : "Enrique" %> </small>
    </div>

</div>


<a href="/goback"><button>Volver</button></a>


<h1>
    Tips:
</h1>

<% if (session.user) { %>
    <% include ../tips/new %>
<% } %>

<% var tips = quiz.tips; %>
<% var quizId = quiz.id; %>
<% var quizAuthorId = quiz.authorId; %>

<% include ../tips/index %>


<a href="/quizzes"><button>Go back</button></a>
