
<h1>
    Random Play:
</h1>

<div>
    Respuestas Correctas = <span id="score"><%= score %></span>
</div>

<p>
    La respuesta <strong> <%= answer %> </strong> es <%= result ? 'right' : 'wrong' %>.
</p>

<p>
    <% if (!result) { %>
    Has fallado.
    <a href="/quizzes"> End of Play </a>
    <% } else { %>
    Has acertado.
    <a href="/quizzes/randomplay"><button>Continua jugando</button></a>
    <% } %>
</p>

